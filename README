TreeView
========

TreeView is a little plugin to generate a tree view. TreeView is implemented by jQuery and jQuery drawing plugin call 


Example
=======

If you have a model with tree structure (the model must has a column name parent_id), you can simple invoke 

<%= build_tree_view(model, attribute name) %> from the view.

The attributes name is the name of the attribute that you want to display on the tree node.


Copyright (c) 2011 Ben Zhang, released under the MIT license
